---
import FlowShowcase from './FlowShowcase';
---

<section id="features" class="section-elev">
  <div class="shell">
    <div class="section-kicker">Commands</div>
    <h2 class="section-title">One binary. Six flows.</h2>
    <p class="section-sub">Commit-message generation is the headline. Everything else — reviews, PR drafts, history rewrites, codebase visualizations — reuses the same config, providers, and prompt pipeline. Pick a tab; the replay keeps looping.</p>
    <FlowShowcase client:only="react" />
  </div>
</section>
